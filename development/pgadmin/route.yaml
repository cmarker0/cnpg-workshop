apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: pgadmin
spec:
  to:
    kind: Service
    name: pgadmin
  port:
    targetPort: 80
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
